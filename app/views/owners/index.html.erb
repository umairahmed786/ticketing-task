<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-body-tertiary">
  <!-- Container wrapper -->
  <div class="container">
    <h1><%= @organization.name %></h1>

      <div class="d-flex align-items-center">
        <% if user_signed_in? %>
          <%= button_to t('logout'), destroy_user_session_path, class: "btn btn-primary me-3", 
                                  method: :delete, data: { confirm: 'Are you sure you want to log out?' } %>
        <% end %>
      </div>
    </div>
  </div>
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->
<div class="container mt-5 bg-light p-3">
  <div class="row">
    <div class="col-md-7">
      <h2 class="mb-4"><%= t('greeting') current_user.name %></h2>
    </div>
    <div class="col-md-5">
      <div class="d-flex justify-content-end">
        <%= button_to 'Invite Users', new_user_password_path, class: "btn btn-primary me-2" %>
        <%= button_to 'Create Projects', new_user_password_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
  <div class="row mt-5">
    
      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-body">
            <h5 class="card-title">Project 1</h5>
            <p class="card-text">Description.............</p>
            <a href="#" class="btn btn-primary">View Project</a>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-body">
            <h5 class="card-title">Project 2</h5>
            <p class="card-text">Description.............</p>
            <a href="#" class="btn btn-primary">View Project</a>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card mb-4">
          <div class="card-body">
            <h5 class="card-title">Project 3</h5>
            <p class="card-text">Description.............</p>
            <a href="#" class="btn btn-primary">View Project</a>
          </div>
        </div>
      </div>


  </div>
</div>
