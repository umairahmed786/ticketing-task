<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-body-tertiary">
  <!-- Container wrapper -->
  <div class="container">
    <h1><%= @organization.name %></h1>

      <div class="d-flex align-items-center">
        <% if user_signed_in? %>
          <%= button_to t('logout'), destroy_user_session_path, class: "btn btn-primary me-3", 
                                  method: :delete, data: { confirm: t('logout_confirmation') } %>
        <% end %>
      </div>
    </div>
  </div>
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->
<div class="container mt-5 bg-light p-3">
  <div class="row">
    <div class="col-md-7">
      <h2 class="mb-4"><%= t('greetings') current_user.name %></h2>
    </div>
    <div class="col-md-5">
      <div class="d-flex justify-content-end">
        <% if can?(:create, User) %>
          <%= button_to t('user_mailer.invite_user.title'), new_user_path, class: "btn btn-primary me-2", method: :get %>
        <% end %>
        <% if can?(:create, Project) %>
          <%= button_to t('projects.create.title'), new_user_password_path, class: "btn btn-primary", method: :get %>
        <% end %>
      </div>
    </div>
  </div>