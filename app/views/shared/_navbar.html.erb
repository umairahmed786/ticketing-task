<!-- Navbar -->
<nav class="navbar navbar-expand-lg" style="background-color: #9DB2BF;">
  <!-- Container wrapper -->
  <div class="container">
    <h1><%= @organization.name %></h1>

    <div class="d-flex align-items-center ms-auto">
      <% if user_signed_in? %>
        <%= button_to 'Logout', destroy_user_session_path, class: "btn btn-primary me-3", method: :delete, data: { confirm: 'Are you sure you want to log out?' } %> 
      <% end %>
    </div>
  </div>
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->