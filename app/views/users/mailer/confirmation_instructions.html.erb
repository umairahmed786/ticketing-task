<p> <%= t('welcome_message') %> <%= @resource.email %>!</p>

<p><%= t('devise.mailer.confirmation_instructions_description') %></p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token, host: "#{@resource.organization.subdomain}.#{APP_HOST}", port: 3000) %></p>
