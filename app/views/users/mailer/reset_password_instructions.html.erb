<p><%= t('welcome_message') %> <%= @resource.email %>!</p>

<p><%= t('devise.mailer.reset_password_instructions_description')%></p>

<p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token, host: "#{@resource.organization.subdomain}.#{APP_HOST}", port: 3000) %></p>

<p><%= t('devise.mailer.reset_password_instructions_ignore_message') %></p>